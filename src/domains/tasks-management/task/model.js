/**
 * @typedef Task
 *
 * @property {string} description.required
 * @property {string} title.required
 */

/**
 * creates a Task
 * @returns {object}
 */
module.exports = () => ({
  name: 'task',
  fields: {},
});
